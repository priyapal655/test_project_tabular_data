# test_project_tabular_data
Enpal case study project testing out some classical ML methods with tabular data for Heat pumps.

## Installation

To install the required libraries, run:

\```bash
pip install -r requirements.txt
\```

enpal_case_demo.ipynb contains all the neccessary codes, plots are saved in the plots directory.